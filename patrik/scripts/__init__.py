import numpy as np
import os
import glob
import torch
import matplotlib.pyplot as plt
import sys

from time import time

code = "Research better and faster approaches\
        Remember to write printing comments in execution (verbose variable documentation) \
        Automate only at the end but write according to it? \
        Write test functions with dummy data \
        Keep track and TODO in technical dept areas \
        "

if __name__ == '__main__':
    print('Program starting')
    start_time = time()
    print('Initiate phase: ...')

    end_time = time()
    print(f'Finished in {(end_time - start_time):.3f} secs')
